plugins {
  id "com.gradle.build-scan" version "1.16"
}

buildScan {
    licenseAgreementUrl = 'https://gradle.com/terms-of-service'
    licenseAgree = 'yes'
}

allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    version = '1.0' 
}